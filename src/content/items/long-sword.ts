// Long Sword — placeholder stub used to verify the catalog loader
// end-to-end. Real item content (slot, stat contributions, hook
// registrations, requirements) arrives with the equipment system in
// session 5+.

import { itemId, type ItemDefinition } from '@engine/index.ts';

export const longSword: ItemDefinition = {
  id: itemId('long_sword'),
  name: 'Long Sword',
};
